# Saved

UE meaning: runtime user/session data.

| Path | Use |
|------|-----|
| `Saved/Logs/` | Logs |
| `Saved/Config/` | Local config overrides |
| `Saved/Crashes/` | Crash dumps |
| `Saved/Screenshots/` | Screenshots |

Debugger working directory is the workspace root. Not shipped in packages.
