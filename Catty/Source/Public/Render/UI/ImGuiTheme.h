#pragma once

#include <Core/Export.h>

namespace Catty
{

/**
 * Applies Catty's default editor chrome.
 * Layered top chrome (menu → tab strip → panel) + black dock chassis gutters.
 */
CATTY_API void ApplyCattyNightTheme();

} // namespace Catty
