# Cached

UE meaning: regenerable derived data (like DerivedDataCache).

- `Cached/Shaders/` — SPIR-V / PSO / shader compile cache
- `Cached/DerivedData/` — imported asset derived data

Safe to delete; not shipped in `Packaged/`.
