---
description: FObject/FPackage must be passed via FObjectRef
alwaysApply: true
---

# 池化对象传递：FObjectRef / FObjectWeakRef

`FObject`（含 `FResource`、`FPackage`）均为池化 FObject。对外一律用 **`FObjectRef`**，禁止裸调 `AddRef` / `ReleaseRef`。

## 写法

- 只有 `FObjectRef`，不要模板特化 / 不要 `FPackageRef`
- 子类型：`Ref.Cast<FPackage>()` / `Ref.Cast<FResource>()`
- `Wrap(ptr)` 仅引擎 / `SetReferencedObjects` 回填用

## FPackage

- Catalog：`unordered_map<string, FObjectRef>`
- 包内对象：`Outer` 为 `FObjectRef`；`GetOuter()` 返回 `FObjectRef`

## 环引用

互持强引用会泄漏 → 一侧用 `FObjectWeakRef`，需要时 `Pin()`。
